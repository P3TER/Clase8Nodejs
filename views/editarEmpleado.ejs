<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <title>
        <%= titulo %>
    </title>
    <style>
        .table-wrapper {
            margin-top: 50px;
        }

        .image-emp {
            width: 40px;
            height: 40px;
        }

        .agregar-empleado-form {
            margin-top: 50px;
        }

        .arriba {
            display: flex;
            flex-direction: row;
        }

        .lados {
            width: 35%;
        }

        form {
            margin-top: 2rem;
        }

        form input {
            margin-bottom: 5px;
        }
    </style>
</head>

<body>
    <div class="page-wrapper">
        <header>
            <nav class="navbar bg-dark">
                <a class="link-light link-offset-2 link-offset-3-hover link-underline link-underline-opacity-0 link-underline-opacity-75-hover"
                    href="/">Empleados</a>
                <a class="link-light link-offset-2 link-offset-3-hover link-underline link-underline-opacity-0 link-underline-opacity-75-hover"
                    href="/agregar" class="float-right" title="Agregar un nuevo empleado">Agregar
                    Empleado</a>
            </nav>
        </header>
        <div class="container">
            <% if(message !='' ){ %>
                <p class="text-center text-danger">
                    <% message %>
                </p>
                <% } %>
                    <% if (empleado) { %>
                        <section class="arriba">
                            <article class="lados"></article>
                            <form action="/editar_emp/<%= empleado.id_empleado %>" method="post" enctype="multipart/form-data" class="card text-bg-dark text-center" style="width: 23rem;">
                                <h2 class="card-title">Editar a <%= empleado.nombres %>
                                </h2>
                                <section class="card-body">
                                    <label for="id_empleado">Id Empleado</label>
                                    <input type="text" class="form-control" name="id_empleado" id="id_empleado"
                                        value="<%= empleado.id_empleado %>" disabled>
                                    <input type="hidden" class="form-control" name="id_empleado" id="id_empleado"
                                        value="<%= empleado.id_empleado %>">
                                    <label for="identificacion">Identificación</label>
                                    <input type="text" class="form-control" name="identificacion" id="identificacion"
                                        value="<%= empleado.identificacion %>" required>
                                    <label for="nombres">Nombres</label>
                                    <input type="text" class="form-control" name="nombres" id="nombres"
                                        value="<%= empleado.nombres %>" required>
                                    <label for="apellidos">Apellidos</label>
                                    <input type="text" class="form-control" name="apellidos" id="apellidos"
                                        value="<%= empleado.apellidos %>" required>
                                    <label for="correo">Correo</label>
                                    <input type="text" class="form-control" name="correo" id="correo"
                                        value="<%= empleado.correo %>" required>
                                    <label for="direccion">Dirección</label>
                                    <input type="text" class="form-control" name="direccion" id="direccion"
                                        value="<%= empleado.direccion %>" required>
                                    <label for="telefono">Telefono</label>
                                    <input type="text" class="form-control" name="telefono" id="telefono"
                                        value="<%= empleado.telefono %>" required>
                                    <input type="submit" class="btn btn-outline-light" value="Editar Empleado">

                                </section>
                            </form>
                            <article class="lados"></article>
                        </section>
                        <% } else{ %>
                            <p class="text-center">No se ha encontrado al empleado. Click <a href="/agregar">aquí</a>
                                para agregar.</p>
                            <% } %>

        </div>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
            integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
            crossorigin="anonymous"></script>
    </div>
</body>

</html>